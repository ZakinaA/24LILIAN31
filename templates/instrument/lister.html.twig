<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
		
		LISTE DES ETUDIANTS</br>
		<table>
		{% for e in pInstruments %}
		<tr>
			<td><a href="{{ path('instrumentConsulter', { 'id': e.id }) }}">{{ e.numSerie }}</a></td>
			<td>{{ e.marque.libelle }}</a></td>
	{% else %}
		<tr>
		<td>Aucun instrument n'a été trouvé.</td>
		</tr>
	{% endfor %}
	
</table>
	<a href="{{ path('instrumentAjouter')}}"> Ajouter un instrument </a>
    </body>
</html>